{
  "name": "blyg-ref",
  "version": "0.3.0",
  "private": true,
  "description": "Blygger reference client — publish, subscribe, and respond (v0.3 'Trunk')",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "export": "node --experimental-strip-types scripts/export.ts",
    "deploy": "wrangler deploy",
    "deploy:all": "node --experimental-strip-types scripts/deploy-all.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^5",
    "hono": "^4",
    "markdown-it": "^14"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "latest",
    "@cloudflare/workers-types": "^5",
    "@types/markdown-it": "^14",
    "@types/node": "^26.1.1",
    "typescript": "^5",
    "vitest": "^4.1.0",
    "wrangler": "^4"
  }
}
